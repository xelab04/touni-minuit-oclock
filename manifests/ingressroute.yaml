apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: touniminui-route
spec:
  entryPoints:
  - websecure
  - web
  routes:
  - kind: Rule
    match: Host(`touniminuit.cloudnativemauritius.com`)
    services:
    - name: touni-minuit
      port: 80
  tls:
    certResolver: le
